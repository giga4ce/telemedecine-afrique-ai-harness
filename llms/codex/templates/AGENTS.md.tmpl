# AGENTS.md — generated from telemedecine-afrique-ai-harness

Do not edit this file by hand. Regenerate it from the AI Harness profile `telemedecine-afrique`.

## Project

Product repository: `telemedecine-afrique`
Current focus: technical teleradiology POC, Tchad-first, Orthanc + DICOMweb + OHIF.

## Stack

- Backend : Python / FastAPI (async), SQLAlchemy 2.0, migrations Alembic — future / conditional stack.
- Frontend : React — future / conditional stack.
- POC : Orthanc + DICOMweb + OHIF Viewer, orchestrés par Docker Compose.

## Read First

In the product repository, read:

1. `README.md`
2. `docs/README.md`
3. `docs/poc/scope.md`
4. `docs/poc/architecture.md`
5. `docs/product/roadmap.md`
6. `docs/domain/legal-reserves-by-country.md`

## Common Rules

Apply the installed shared rules from `.codex/context/project-rules.md`, the POC boundaries from `.codex/context/poc-boundaries.md` and the Jira rules from `.codex/context/jira.md`.

## Jira / MCP

The Atlassian MCP server `atlassian` is declared in `.codex/config.toml` (transport HTTP, no secret). Authenticate once per machine via OAuth. Follow the traceability rule in `.codex/context/jira.md`: open a Jira ticket (project `KAN`) after each completed task.

## Installed Skills

Use the installed skills under `.codex/skills/` when they match the task.

## Codex Notes

Before editing, inspect the relevant files and current Git state. Keep changes scoped to the requested task. Do not create tickets, branches, commits or pushes unless the user explicitly asks.

Posture pédagogique : après l'implémentation d'un ticket, et sur demande de l'utilisateur, explique pédagogiquement ce qui a été construit — rôle de chaque fichier significatif, principes SOLID réellement illustrés, aspects sécurité, idiomes Python/FastAPI notables (type hints, async/await, Pydantic, context managers, décorateurs). L'utilisateur est un développeur PHP/Symfony senior en apprentissage de Python : ancre chaque concept nouveau sur son équivalent Symfony/Doctrine quand c'est pertinent (Depends() ↔ container Symfony, schéma Pydantic ↔ DTO + Assert, migration Alembic ↔ migration Doctrine, dépendance de rôle ↔ Voter). Tu enseignes, tu ne juges pas la qualité et tu ne proposes pas de modification à exécuter : une amélioration se formule comme piste de réflexion. Même esprit que le sous-agent `mentor-python` côté Claude, sans mécanisme de sous-agent dédié.
