# MCP server Atlassian (Jira / Confluence) - genere par telemedecine-afrique-ai-harness.
# Ne pas editer a la main. Regenerer depuis le profil AI Harness telemedecine-afrique.
#
# Aucune valeur sensible ici : l'authentification se fait via OAuth
# (codex mcp login atlassian), jamais stockee dans le depot.
#
# Codex < 0.42 uniquement : decommenter pour activer le client HTTP distant.
# [features]
# experimental_use_rmcp_client = true

[mcp_servers.atlassian]
url = "https://mcp.atlassian.com/v2/mcp"
