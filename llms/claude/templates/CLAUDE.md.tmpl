# CLAUDE.md — generated from telemedecine-afrique-ai-harness

Do not edit this file by hand. Regenerate it from the AI Harness profile `telemedecine-afrique`.

## Project

Product repository: `telemedecine-afrique`
Current focus: technical teleradiology POC, Tchad-first, Orthanc + DICOMweb + OHIF.

## Stack

- Backend : Python / FastAPI (async), SQLAlchemy 2.0, migrations Alembic — future / conditional stack.
- Frontend : React — future / conditional stack.
- POC : Orthanc + DICOMweb + OHIF Viewer, orchestrés par Docker Compose.

## Read First

In the product repository, read:

1. `README.md`
2. `docs/README.md`
3. `docs/poc/scope.md`
4. `docs/poc/architecture.md`
5. `docs/product/roadmap.md`
6. `docs/domain/legal-reserves-by-country.md`

## Common Rules

Apply `profiles/telemedecine-afrique/context/project-rules.md`, `poc-boundaries.md`, `workflow.md` and `repository-map.md`.

## Claude Notes

Claude sub-agents live in `llms/claude/agents/` (`backend-fastapi`, `frontend-react`, `dicom-integration`, `devops-infra`, `tests-qualite`, `securite-conformite`). Use them only when their task applies. The FastAPI backend and the React frontend are future or conditional stack choices until the product repository initializes them.
